# Dependencies (tải lại được bằng npm install — KHÔNG up lên GitHub)
node_modules/

# Cấu hình máy cá nhân / trình soạn thảo (không cần đẩy lên)
**/settings.local.json
.vscode/

# Linh tinh hệ điều hành
.DS_Store
Thumbs.db

# Công cụ chạy thử preview nội bộ (không cần trên GitHub)
.claude/
